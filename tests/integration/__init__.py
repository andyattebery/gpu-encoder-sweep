"""The integration suite: the queue contract and the agent protocol against a real Redis, under `make integration` with
SWEEP_TEST_REDIS set to a redis:// URL of a database the suite may flush. Not part of `make check`."""

"""sweep/node -- the agent: it claims a plan from the hub, runs the argv it was handed, and posts what came out.
It composes no flags and holds no credential but its own token; everything else comes from the hub."""
